prototype is the project for detecting design errors and recognizing potential requirements.
ShareDoc is the project currently being developed.


ShareDoc:
.py 3063
.js 806
.css 1426
.html 1032


Currently Working On:
# Allow a projcet manager to remove a group of user from the project.
# Add style to all page.[50%]
# Replace user gravater/name from plain text to link, with changing the preseting style.[50%]


To Do List:
# In BasicInfoHandler, It's not necessary to return the sucessfully modified message. Fix it.
# refactor _get_html_response, _group_manager_list_handler, _group_member_list_handler of GroupPage and GroupManagementPage.
# Paging on messages.
# Paging on ac search results.
# Runtime retriving the messages.
# Add commentation function on posted message.

Shelved:
# Replace the transportation machanism from JSON to plain html.[shelved]
# Create registration page.[shelved]
# Fix bug on md5 extraction on client side.[shelved]
# Add notification for project/group removing a user or changing sth.[shelved]


Finished:
# Add progress bar to file upload process.[finished]
# Find out how to terminate the file upload process.[finished]
# Add condition on posting message, for preventing empty message.[finished]
# Create user page, replace the plain user name with the link to its user page.[finished]
# Enable modify on posted message.[finished]
# Redesign the notifacation center.[finished]
# Give notification after someone process a message.[finished]
# Adjust message creation widget, prevent reloading file list after successfully uploaded a file.[finished]
# Build login page. Passing md5 of password to server.[finished]
# Fix the username problem. User should use email to register and login, use customized name(for instance, a real name) to communicate. username of User model should be a unique name that generated by system.[finished]
# Set link to gravater.com with user gravater in user page.[finished]
